= form_with(model: ingredient, local: true) do |form|
  - if ingredient.errors.any?
    #error_explanation
      %h2
        = pluralize(ingredient.errors.count, "error")
        prohibited this ingredient from being saved:
      %ul
        - ingredient.errors.full_messages.each do |message|
          %li= message

  %div{class: "fl w-50 pa1"}
    %label Name
  %div{class: "fl w-50 pa1"}
    = form.text_field :name

  %div{class: "fl w-50 pa1"}
    %label Units
  %div{class: "fl w-50 pa1"}
    = form.select :units, Ingredient.units.keys

  .actions{class: "fl w-50 pa1"}
    = form.submit
